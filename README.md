This is an app for creating autos. This app is specifically designed for FRC and for the team CPR 3663.

---

To make a new release on GitHub just run (with whatever version it should be):
```cmd
git tag v1.2.3
git push origin v1.2.3
```